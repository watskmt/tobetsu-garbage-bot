[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B"]
ignore = ["E501", "B008"]  # B008: FastAPI の Depends() は引数デフォルトで使う慣習

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]   # CI の `pytest -q` でもリポジトリ直下のモジュールを import できるように
